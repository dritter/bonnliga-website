{% extends "KcbBonnligaWebsiteBundle:Layout:base.html.twig" %}

{% block body %}
    <div class="page-header">
        <h1>{{ post.title }}</h1>
    </div>

    <div class="well">
        {{ post.content|raw }}

        <br /> <br />
        <code>Veröffentlicht am {{ post.createdAt|date("%d.%m.%Y um %T") }} Uhr von <strong>{{ post.author }}</strong>.</code>
    </div>
{% endblock %}
